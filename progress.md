# Session Progress Log

> 📢 本文件是**当前状态快照，整体重写不追加**。历史都在 git（`git log` + 各 `.issues/*/receipt*.md`），别在这儿堆编年史。
> 启动路径见 `AGENTS.md` Startup Workflow：读本文件 + `feature_list.json`，跑 `./init.sh` 确认绿，再开工。

**Last Updated:** 2026-08-07 晚（T8 收官自检 → 统一上产 → HITL 端到端轮 → HITL 三条修复补完上产；
当晚复盘 + grill 五拍板 → **gap2 战役开票 #58/#59/#60**，全排碰头后）

## Current State

- **git**：`main` = 差距战役 #50–#57 八票全在。T8（#57）是自检票，除回执与语料外**改了一处产品代码**
  （`avery/memory.py` 中文分词）+ **一条新门**（`tests/test_cjk_recall.py`）。
- **验证账实（本轮实测，非估数）**：后端离线全套 **3798 passed / 107 deselected / 4 xfailed / 0 failed**
  （T8 基线 3785 + HITL 补的 13 条门）；
  `@needs_db` 全套 **98/0**（真 PG，8m02s）；前端电池 **A 25/25 · B 3/3 · C 3/3**；像素 40 张全绿
  （其中 4 张 files 是本轮人审后重冻）；i18n 898 叶子键 / 孤儿 0；
  端到端 `t8-e2e.mjs` zh/en/zh **连续三轮零新发现**。
- **✅ 已统一上产 + HITL 复验（2026-08-07）**：回执两份——
  `receipt-deploy-0807.md`（上产）与 `receipt-hitl-0807.md`（HITL 轮，含三条修复与一次凭据事故）。
  后端容器今天换了三次：`main-20260805-160609` → `-20260807-142044`（T1–T8 上产）
  → `-153354`（自述取证闸 + 工号）→ `-164759`（规则 3.5）
  → **`main-20260807-190332`**（= main `99d83f7`，铸链绑项目 + 工号接进界面）。
  回滚梯逐级保留。前端线上 = **main HEAD**（push 即自动构建；
  上产当时核实到的是 `4650e1e`，此后每次 push 都会把它顶成新的 HEAD——**核实方式**见回执，
  别记具体 SHA）。
  迁移 0012/0013/0014 已在生产库落地（`form_templates`/`form_submissions`/`project_ref` 列、
  `entities.kind` 含 `conflict`、person 白名单含 `person_id`/`self_report`/`provenance`）。
  ⚠ **口径纠正**：前端**没有人工上产步骤**（push main 即自动构建上产），只有**核实步骤**——
  progress 旧版里「89b36e4 / fa8d085」是两次相邻的自动构建，不是漏部署。
  ✅ **行为证据已拿到**（HITL 轮）：Danny 手机真填一份周报 → 资料库/人卡/今天页/议事室四处对上；
  修完三条之后重跑同一批语料，生产上 6 人一人一格、工号逐字进卡、零编造自述。

## 本轮做完的（2026-08-07 · T8 收官自检，issue #57）

回执：`.issues/gap-design-0805/receipt-T8-e2e.md`（逐轮发现 + 门 + 变异 + go/no-go）。

- **十段剧本端到端**（`t8-e2e.mjs`，真浏览器打真本地后端 + 真 PG）：传材料 → 回拨时间 →
  今天页三规则 → 冲突关闭出口 → 建链 → 员工填（正常/模仿攻击/同名歧义/过期/重复）→
  资料同构 → 回流人卡项目卡 → 今天页复检 → 议事室引用。语料在 `t8-fixtures/`，截图在 `t8-shots/`。
- **逮到一条真产品缺陷并修掉**：关键词 recall 对**中文语料恒返回空**
  （`memory._tokens` 只认 `[a-z0-9]+`）。mock 侧落空会兜底成一条形状完美的 `facts.md:1` 引用，
  所以七票加起来没红过。生产今天走 DashScope 语义排序不受影响，但 key 缺失/轮换的**降级路径**
  对中文客户＝零证据。改法：中日韩 bigram 分词；门：`test_cjk_recall.py` 9 条。
- **另外三条都是「门自己的锅」**：两条判据选择器/签名写错、两条空判据（`grade==='clear'` 真值是
  `can_proceed`；⑥b 选择态串批导致同名提交压根不存在）。后者是**变异测试**翻出来的。
- **像素基线陈货**：`*-files-*` 4 张停在 07-29/08-02（T3 在自己 worktree 里重冻过，
  `__snapshots__/` 是 gitignore 单机产物、每 worktree 一份，主检出从没更新）。人审后重冻。

## 本轮 HITL 逮到并修掉的（2026-08-07 · 回执 `receipt-hitl-0807.md`）

**T8 全程用离线替身抽取器，HITL 第一次让真 MiniMax 走完整条链**，当场三条：

1. 🔴 **人卡上出现没人填过的「自述负载 0%·本人自述」**——模型不听提示词，读着「负责人：徐岚」
   就吐了条 self_report。判据只写在提示词里＝没有判据；已加**取证闸**（读数要有一行同时含
   名字+自述标记+这个值的原文撑着，找不到整条丢弃，source 重指到那一行）。
2. 🟠 **同名两位「林小满」并成一张卡**——schema 没要过 `person_id` + 文档内归并只按姓名
   （T5 把跨文档换成 PersonIndex 后两条腿分叉了）。已补字段 + 两边读同一本索引。
3. 🟠 **修完 ② 后冒出幻影第三张卡**——规则 3 第一次被真触发。纪要自己写了「（前厅部）」，
   所以按**规则 3.5**：恰好一位候选部门对得上就并进去，对不上仍退回老口径不猜。

4. ✅ **铸链绑项目 + 工号接进界面**（第三条差距 + 第二条后半截，当日补完）：
   逐人一格的项目下拉、默认不绑；有工号送工号、没有仍送空串；「谁交了」显示「关于 X」；
   重名的人在 chip 上补部门（这条是**截图人眼过时逮到的、本次改动自己造的**——
   同名两位正确分开之后，选人那排出现两个一模一样的名字）。

四条都做过变异测试或自证判据，13 条新门，全部已上产并在生产复验
（前三条重跑同一批语料，第四条走示例团队零花费验渲染与契约）。

## What's Next（按优先级）

1. **gap2 三票先做完**（0807 深夜 Danny 改口：先票后演习；kickoff 卡片已发，每票一个 AFK session；
   档案 `.issues/gap2-0807/tickets.md` = 0807 grill 五拍板 + 共享事实图）：
   #58 T9 站内主动（流量触发自动补铸·照抄上期可改 + 'form' 通知 + 今天页缺交规则）·
   #59 T10 补资料门（追加上传 + 实体增量归并·安静更新，**不许砍半**）·
   #60 T11 模板拼装器（建/改模板 UI + yes/no 与 1~5 + Avery 读旧表格起草）。
   三票互相独立，但都动 FilesScreen——**合 main 串行、每合一张在主检出重冻像素**。
   做完统一上产，然后才轮到第 2 条的演习。时间不够的退路 = 演习对着现状走、三票挪碰头后。
   明确不开票：邮件通道、03 表指标卡、模板版本快照、议事室引用命名（仍等拍板）。
2. **Danny 用户视角端到端演习**（三票落地上产之后）：从零自己开一个新工作区走全程，
   记录「卡在哪、哪一步不知道该点哪」当清单，演习后再定修哪些。
   （原「三亚 demo 脚本」一项已取消——0807 Danny 拍板，不做讲稿。）
   ⚠ 复盘时纠了我一句错话：「数据规范和表格」**已进产品**——`/paperwork` 页就能下载合伙人那套
   01–07 表（入口在上传框旁），残余差距只有「03 表不长成指标卡」。
3. **T8 留的两条记录还在桌上**（只记录未改，等拍板）：
   ① 议事室引用编号是 `facts.md:<行号>` 而非客户自己的文档名（人卡/今天页已是 ADR-0028 形状）；
   ② 今天页证据行是 `字段="值"` 的机器形状（ADR-0033 明定），演示时建议口头带一句怎么核对。
4. **给 `/health` 加一行版本字段**（镜像 tag 或 commit）：现在「生产跑的是哪个 commit」外部不可核，
   每次上产都要 `docker exec` 抠文件指纹自证一遍。小改动，下次上产顺手带。
5. carry-over：r2 未开票发现（`.issues/sweep/2026-08-02-r2.md`）· gate-run 迁移（aria-zh/cr-alignment）·
   files-hub #26–#29 · 换血抢救 #31/#32 · v01 退役 #33 · 真机零覆盖（iOS/微信，最高优）·
   成本票 #30 等真实客户量 · 真 brain 分流取证（要先给口径）· 全量 feat-063。

## Blockers / Risks

- 无硬 blocker。
- 🔴 **脚本落盘的「会话/状态」文件默认当凭据看**：`hitl-setup.mjs` 存的 playwright storageState
  里有生产 context 的 `ownerToken`。0807 我手工备份的 `.hitl-session-round1.json` 没被旧的
  `.gitignore` 写法覆盖，随 `4d74ebd` 推上了**公开仓库**。处置＝**删掉那个 context**
  （token 从此指向不存在的东西＝真吊销；改 git 历史在公开仓库里靠不住，fork/缓存都可能留）。
  规则已收紧为 `.issues/**/.hitl-session*.json`。**先写 .gitignore 再写脚本**。
- ⚠ **提示词里的约束不是判据**：模型会不听。凡是「只有满足 X 才准输出 Y」的规矩，
  落地层必须有一道能对着**文档原文**验的闸（0807 的自述取证闸就是这么补的）。
- 🔴 **stale uvicorn 按端口杀**（`Get-NetTCPConnection -LocalPort <口> -State Listen` → `Stop-Process`），
  别信 ps 信行为；各 session 结束清自己起的端口。
  同族：`PgRegistry._schema_ready` 是实例级缓存——换库不重启后端＝迁移不重放。
- 🔴 **`vite preview` 默认只绑 `::1`**：`localhost` 通、`127.0.0.1` 拒。写死 127.0.0.1 的门
  （`verify-null-owner`）会以「连接被拒」的形态假红，而吃 `VERIFY_BASE` 的 A 区门全绿——
  **A 区全绿不代表 preview 绑对了**。起 preview 一律加 `--host`。
- 🔴 **在 worktree 里重冻像素＝没重冻**：`__snapshots__/` 是 gitignore 的单机产物、每 worktree 一份。
  改了界面的票合回 main 后，必须在**主检出**再跑一次像素门；红了先查 mtime 再谈漂移。
- 🔴 **本机 Docker PG 容器时钟来回跳 ~115s**：`created_at < now()` 判据间歇假红；
  要「够旧」就显式拨时间（`now() - make_interval(days => N)`，用 pg 自己的 now()）。
- 🔴 **本机 curl 把 argv 中文按 GBK 编**：中文只走 heredoc / 预先 percent-encode / `--input` 文件。
  同源坑：`python -c "…中文…"` 也会被啃——中文脚本写成 .py 文件再跑。
- 🔴 `e535ec9` commit message 是错的（真相在 `03a9824` erratum）；rebase 与否归 Danny。
- 🔴 repo 级 stash 两条别人的存货（`stash@{0}`/`stash@{1}`）；处置归 Danny。
- 多个战役 worktree 仍挂着（T1–T7 各自的 + 更早六个）——删分支/worktree 属删除闸，归 Danny。
- ⚠ 本机残留：Docker 库 `t8e2e`（T8 自检专用，与 teammaster/生产无关，留着可复查）；
  `teammaster-postgres-1` 容器在运行态。
- ⚠ **各票收尾必须重写本文件**，这是 AGENTS.md 的 DoD，不是可选项。
