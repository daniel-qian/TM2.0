node_modules
dist
dist-ssr
*.local
.vite
.DS_Store
*.tsbuildinfo

# Agent-local state. Keep project hook/config paths trackable by default:
# .claude/settings.json, .claude/hooks/, .codex/hooks.json, .codex/config.toml, .codex/hooks/
.claude/settings.local.json
.claude/worktrees/
.claude/cache/
.claude/archives/
.claude/launch.json
.codex/local/
.codex/cache/
.codex/archives/
.vercel
.env*.local

# feat-046 · 双皮 review 证据包（截图/计算值清单）——给 Danny 的 review 包素材，不进 git
.issues/lite-live-v02-0713/review-shots/
# feat-047 e2e seed 文件（鹿山雅居虚构数据，供 Danny 手动上传测真 LLM 抽取）——测试产物不入库
.issues/lite-live-v02-0713/e2e-seeds/
# 门驱动辅助（gate snippet / evidence collector 的 public/ 临时拷贝）——收口必删，防漏兜底
public/__gate_verify.js
public/__evidence_collector.js
# 跑动产物（uvicorn 日志、Playwright 驱动与结果 JSON）——运行时输出，不入库
runs/
# cr-align 战役跑动产物（UI 扫雷 JSON、对照板 PNG）——台账 ui-sweep-triage.json 是 tracked 的，产物不入库
eval-harness/reports/
# 像素基线：单机字体渲染产物（11MB+），不入库；换机用 --update-snapshots 重采（同 review-shots 先例）
eval-harness/visual/__snapshots__/
eval-harness/visual/test-results/
# playwright runner 的失败产物目录（落仓库根）
test-results/
# 会话复盘存档（transcript-zh 产物）——本地留存，不入库
transcripts/
.vite-gate-cache/
vite.out
vite.err

# rich-align-0722/10 · 登录隔离演示台的临时 demo dist（runtime，不进 git）
dist-authdemo/

# partner-docs-0728 · scripts/ 下跑 python 会落这个（make-intake-xlsx.py）
scripts/__pycache__/

# 走查产物：截图 + 逐屏 JSON 快照，单机产物、体量大，不入 git（round-1 的 2026-08-02-shots 同样惯例）
.issues/sweep/*-shots/
