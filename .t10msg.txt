feat(#59): T10 补资料——追加上传 + 实体增量归并 + 跨期对比接通

拆掉「每次上传=新开一家公司」那堵墙。

后端
- 新端点 POST /team/{context_id}/files：authorize_context 鉴权，字节侧闸与 /ingest 逐条同一套；
  `upload_guard._route_for` 补一条 /team/*/files 分支（那张表是精确匹配，带路径参数的路由
  永远命不中——漏了这条新端点在 ASGI 边缘就是零防护）。
- 新模块 avery/ingest/file_append.py：照 form_append 的 get→原地 mutate→put 范式，
  绝不调 ingest_paths/ingest_docs（覆盖语义）、绝不新造 CompanyContext（pg put 是快照替换）。
  只对新文件跑抽取——LLM 花费与新文件成正比，不与资料总量成正比。
- extract.py：提出 `_absorb_project` / `_absorb_self_report`（上传路与补传路共用一份定义），
  新写 `_disambiguate_project_ids`、`AppendLedger`、`merge_project_reading`、
  `dedupe_signals_after_linking`；`merge_person_reading` 加可选 ledger——账本在场时 team 成为
  合法载荷，PersonIndex 规则 3.5（同名+部门消歧）第一次真的够得着。
- merge_person_reading docstring 的四个坑逐条落成机制并逐条转成判据：整表重写吞手编/软删/出处、
  旧冲突重复记账、held_src 记错、signals 换尺重筛。
- 安静更新：只有确凿知道新资料更新时才让新值顶掉旧值（拿不准一律 keep-first）；手编恒赢，
  但与手编矛盾的读数照样记账不吞；逐字段出处写进 provenance 侧车（origin='doc'）。
- registry/pg_registry/Protocol 各加 is_ephemeral()，/team/{id} 每帧投 `ephemeral`。

前端
- 资料库屏第②段拆成两个动作：「给这家公司补资料」（append）与「另建一份画像」（new），
  againTitle/againBody 跟着改口——以前"合并"根本不存在，现在存在了，再说同一句就是指错按钮。
- UploadPanel 加 mode；store 加 appendFiles 与**自己的**状态机（借 ingestStatus 会发一条
  假的「你的团队已就绪」通知）；transport 加 appendFiles。
- provenanceBadgeKind 加第四态 'doc' → 详情浮层显示「读自〈文档名〉」（只印文档名不印行号）。
- 一次性示例克隆按后端 `ephemeral` 禁入口（不做假按钮，留一句说明）。
- i18n zh/en 同批 9 键，孤儿 0。

门
- 离线 45 条新判据（tests/test_file_append_t10.py），全套 3843 passed。
- @needs_db 4 条新判据（含「补传后旧手编不丢」「冲突跨批不翻倍」），全套 102 passed。
- 浏览器 story 门 verify-append-story.mjs 18 判据，已入 ROSTER A 区（上传型门）。
- 变异测试 18 条后端 + 3 条前端全部 killed；其中一条前端变异翻出门洞（story 门原先直接调
  store、没碰过真按钮），已改成走真界面入口后补杀。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
