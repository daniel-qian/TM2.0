# 第一波差异清单 · v02(lite2) vs 合伙人版(manager-command-room)

> ## 🔴 2026-07-19 更正 · 本文基线早于 feat-050..060
>
> **基线是 `feat/047-v02-engine-sync @ 1833d97`，即十一条（feat-050..060）落地之前。**
> 那十一条现已全部合入 `main` 并上线（`origin/main` = `de47ffe`，裸链默认已翻成 v02）。
> 本文作为 `1833d97` 的快照仍准确，**但不能用来回答「我们现在有没有 X」**。
>
> 与本文判定直接相关的变化：
>
> * **上了 router**（feat-051）——「无路由」类判定全部失效；深链、后退键都有了。
> * **屏数 7 → 9**：新增 `/projects`（feat-055）与 `/home`（feat-057），**`/home` 是默认落地屏**。
> * **有站内草稿框了**（feat-058 `DraftComposer.tsx`）。
> * **`skin` 已更名 `look`**：`?skin=` → `?look=`、`data-skin` → `data-look`、`skin.ts` → `look.ts`
>   （feat-068，因与 ADR-0021 的 `Skin` 撞词）。⚠️ **旧的 `?skin=` 刻意不做别名**——
>   `?skin=aurora` 只报 `console.error`，页面仍渲染默认 `paper`。**老脚本会假通过**：
>   自以为验了极光皮，实际验了两遍 paper。本文 §「双皮」相关行里的 `skin.ts` 请一律读作 `look.ts`。
> * **`§0` 里「实施分支从哪个基线切」这条阻断已作废**：活全落在 `main` 上。
>   （对应 addendum 的开放问题 O1，那边也标了。）
>
> 原文一字未删，保持可追溯。

> 本文只做「差异清单」，**不写实现代码、不建实施工作树**。判定列是我(agent)的建议，Danny 审完再定第二波(grill + PRD/ISSUES)。

---

## 0 · 方法 / 基线 / 证据来源

**基线**
- 我们(ours) = `feat/047-v02-engine-sync @ 1833d97`，代码看 `src/lite2/**`(v01 的 `src/lite/**`、禁区 `src/story/**` 未看)。为不动共享检出，另开只读工作树 `.claude/worktrees/v02-baseline-ro`(detached 1833d97)读+跑。
- 合伙人(partner) = `D:\avery-command-room (2)\avery-command-room`(Next.js `manager-command-room`)，当前检出，只读、未提交、未 git 操作。全是 `lib/data.ts` 里的 mock，刷新即还原、无后端无鉴权。

**怎么做的**
1. 两个只读 code-mapper 子 agent 出全量交互清单 → [`partner-inventory.md`](partner-inventory.md)(225 行)、[`ours-lite2-inventory.md`](ours-lite2-inventory.md)(261 行)。
2. Playwright/浏览器同时驱两版：
   - 合伙人 `:3100`(mock，开箱有料)——走了 `/`、决策队列展开+行动、`/nexus` 对话。
   - 我们 `:5173/?v=2&mode=live&skin=paper&lang=zh` + 本机后端 `:8137`(已有一个 `AVERY_BRAIN=minimax` live 实例在跑，真 key 在 `eval-harness/.env`)。用**真上传**把三亚 seed(鹿山雅居 花名册/项目周报/周例会纪要/绩效REDLINE探针)灌进去 → `POST /ingest` 真 minimax 抽取，**20 人 / 17 项目**，`ctx_b3f9d526e924`。7 屏全部现场走了一遍。

**证据标记**：`[LIVE]`=本次浏览器实测；`[CODE]`=代码清单(file:line)；`[PAYLOAD]`=真 ingest 回包。

**硬约束回执**
- 🔴 **人卡不做分数血条**：真回包顶层键 `context_id/source_files/people/projects/briefing/signals/owner_token`，`"score"` 出现 **0 次**；人对象键 = `id/name/role/team/tenure/owns/collaboration`——**结构上就没有分数字段** `[PAYLOAD]`。我们 UI 也明写"不给任何人打分" `[LIVE]`。
- 后端接线走 `VITE_AVERY_API_BASE`(缺省 `http://127.0.0.1:8137`)，`owner_token` 只进 header 不进 URL `[CODE transport.ts:237,279]`——没写死隧道。
- 截图：浏览器 pane 的 screenshot 反复 30s 超时(framer-motion 让渲染器一直"忙")；证据改用 DOM a11y ref + page text(任务允许"截图或 DOM ref")。

---

## 1 · 屏幕对应表(合伙人为基准)

| 合伙人(路由) | 我们(lite2 屏) | 关系 |
|---|---|---|
| `/` 指挥室(仪表盘) | `你的团队` + `议事室` 拆成两块 | 概念错位：她一屏聚合，我们分屏 |
| `/companyinput` 上手(独立整页 5 步) | `OnboardWizard`(浮层 4 步) | 都有，形态不同 |
| `/nexus` 对话框 | `议事室` 画布 | **并存**(Danny 指定) |
| `/people` + 人卡弹层 | `你的团队` 人员分组 + `DetailOverlay` | 都有，她带分数/我们不带 |
| `/projects` + 项目详情弹层 | `你的团队` 项目卡 + 详情 | 都有，她更全(增改归档) |
| `/gaps` 现实差距 | `多看一眼` | 都有(平级) |
| `/checklist` 待办 | `跟进` | 都有(平级) |
| `/playbooks` 方法库(6 卡) | `操作手册`(即将推出占位) | 我们没做 |
| 决策队列(在 `/` 内) | `你的团队`→`今天值得你留意` 卡 | 都有(平级) |
| 通知铃 + 下拉 | `通知`(LiteBell) | 都有 |
| 顶栏全局搜索 | 无 | 我们缺 |
| `问 Nexus` 全局 FAB | 无(议事室是一个 tab) | 我们缺全局入口 |
| 无 | `未来方向`(产品宣言) | 我们独有 |
| 无 | `Avery 的笔记`(观察实录) | 我们独有 |

---

## 2 · Danny 点名的重点(先看这四条)

| # | 她有的 | 我们现状 | 判定(建议) | 理由 / 证据 |
|---|---|---|---|---|
| 2.1 起草消息弹框 | 点决策行动(message/delegate 型)或人卡「发消息」→ **弹一个应用内草稿预览弹层**：收件人 chip + 主题 + 预写正文(只读)，选 `邮件`(mailto)/`聊天应用`(复制到剪贴板)/`完成—加入待办` | 「起草消息」是一个**裸 `mailto:` 链接**，预填 subject+body，点了直接甩给系统邮件客户端，**不弹任何应用内框** | **补** | 正是 Danny 说的"点起草消息该弹消息草稿框(我们没弹)"。`[LIVE]` 我们 closer-look 卡的「起草消息」= `<a href="mailto:?subject=…&body=…">`(ref_65/75/85…)；`[CODE]` `draftLinks.ts`(mailto 生成)、`action-modal.tsx:131-342`(她的弹层，只读预览) |
| 2.2 议事室"简化输出" | **两边都没有**这个开关 | 议事室「thinking it through」终端把每条流事件**逐行原样打**(read_case / recall / TOOL 大段 facts.md 转储 / cite / draft_advice)，**英文脚手架 + 中文事实混排**，无折叠无过滤 | **补(净新增，非对齐缺口)** | Danny 说"the room 思考过程该有简化输出(我们没有)"。`[LIVE]` 现场那条问答思考区滚出几十行 `recall query=… / facts.md:167 Project '…' at-risk / ✓ cited: «…» ⟵ facts.md:169`，对经理是开发视角的墙。**注意**：最终建议卡 + `Avery的笔记` 都是干净中文——脏的只有实时思考流。她的 Nexus 推理链(4 步)也是永远全展开、无简化。所以这是"两边都缺、我们更该做"的净新增 `[CODE]` `streamSource.applyEvent` 全量不过滤 |
| 2.3 画布 vs 对话框 | `/nexus` = 对话框(消息流 + 输入 + 发送 + 建议问题 chip + 打字指示 + `?q=` 自动问) | `议事室` = 画布(拖动平移·滚动缩放·重置视图) + 思考终端 + 建议卡；提问框在画布外常驻 | **并存(不二选一)** | Danny 指定两套设计并存。`[LIVE]` 双方都实测到。落点：保留我们画布，另外把"对话框式"体验补进来(或让议事室支持对话流视图),而不是拿一个换另一个 |
| 2.4 人卡分数血条 | 人卡详情有 **ScoreRing(总分)+ 5 条 DimensionBar(业绩产出/协作/主动性/可靠性/状态健康，各带分)+ TrendChart(负载/产出双线,5周/季度/半年/1年切换)** | 人卡只有 name/role/`Owns …`/collaboration，**无任何分数** | **不做(硬约束 + 红线)** | 🔴 真回包无 score 字段 `[PAYLOAD]`；`未来方向` 屏白纸黑字"人永远不会被打分、被排名、被画像；任何指令都关不掉这条" `[LIVE]`。她这块**整块不搬**。若要补人卡深度，只能沿 `owns/collaboration/tenure` 这种"做什么、扛什么"的**非评分**维度做 |

---

## 3 · 逐屏差异

判定：**补**=她有我们该加 / **并存**=两套都留 / **不做**=不搬(约束或不合我们方向) / **平级**=大体对齐,细节可选。

### 3.1 上手 Onboarding

| 她有的 | 我们现状 | 判定 | 理由 / 证据 |
|---|---|---|---|
| **独立整页** 5 步向导(自带 `layout.tsx` z-9999 盖住 shell) | **浮层弹窗** 4 步向导(auto-open 首访；×/Esc 暂停记步；跳过=不再弹) | 平级(形态之争留给 Danny) | `[CODE]` 她 `companyinput/page.tsx:59`；我们 `OnboardWizard`+`onboardStore`。`[LIVE]` 我们浮层实测:step1 上传→"团队已就绪"→下一步/跳过 |
| 步骤：①上传 ②**连接工作工具**(企微/飞书/钉钉/OA/BI/CRM 6 个开关) ③团队信息 ④选方法(8 选,预勾 3) ⑤**创建账号**(邮箱+密码) | 步骤：①上传(真 ingest) ②团队信息 ③选方法(8 选,默认 3) ④完成小结 | 补:**②连接工具** 这一步我们没有 | `[CODE]` 她 `page.tsx:284-332`(工具开关)、`:472-545`(建账号)。工具集成=真价值点;建账号属鉴权,原型阶段可缓 |
| 上传步骤:拖拽+点选,已传文件列表带逐个删除 | 同(拖拽+点选);真上传后回"你的文件"列表带"N 处引用" | 平级(我们其实更实:真抽取+引用计数) | `[LIVE]` 我们 4 文件→"7.1 KB · 21 处引用"等 |

### 3.2 落地页/首屏(她:指挥室 / 我们:你的团队+议事室)

| 她有的 | 我们现状 | 判定 | 理由 / 证据 |
|---|---|---|---|
| **KPI 条** 5 张业务指标卡(营收完成率58%/预订转化66%/投诉率4.2%/团队负载72%/未解决差距) | 只有"团队一览"计数(people/active projects/need a look) | 不做(暂):无数据 | 她这 5 个是 `lib/data.ts` 写死的 mock;我们 payload 里没有营收/转化/投诉这类指标,`signals` 是定性信号非 KPI。要补得先有数据源 `[PAYLOAD]` |
| **决策队列** "今天需要您决策的事项(5 待处理)":手风琴,展开=证据 + 推荐方法 + 行动按钮 + `深入问 Nexus`(带问题跳对话) | `今天值得你留意`:11 张 closer-look 卡,每卡=证据描述 + `打开项目`/`带进议事室↗`/`加到跟进`/`起草消息`/`今天先放放` | 平级(结构高度重合) | `[LIVE]` 两边都实测。她"深入问 Nexus"把决策问题带进对话 ↔ 我们"带进议事室"把卡带进画布——同构。细节可互相取长(见 3.3) |
| **报告 vs 现实** gap rail(整行跳 `/gaps`) + **需要关注的成员** people rail(点开人卡) | 差距在 `多看一眼`;人员在 `你的团队`(不单列"需关注") | 平级/可选 | 她首屏把"gap + 需关注人"聚合到右栏;我们分屏。是否要一个聚合"晨间首屏"值得 Danny 定 |

### 3.3 决策 / 行动 / 起草(核心交互)

| 她有的 | 我们现状 | 判定 | 理由 / 证据 |
|---|---|---|---|
| 行动按钮按 `action-defs.ts` 分 4 型:**book**(会议→Outlook/Google 深链)、**delegate**(委派事项列表+草稿)、**message**(草稿)、**confirm**(单确认) | closer-look 卡行动:`打开项目`/`带进议事室`/`加到跟进`/`起草消息`(mailto)/`今天先放放`;无 book,无委派,无 confirm 型 | 补:**book(约1:1/会议深链)**、**委派/确认型行动** | `[CODE]` 她 `action-modal.tsx` 4 型;`action-defs.ts` 每决策挂具体行动。我们行动集偏"看/跟进/问",缺"排期/委派/拍板" |
| 行动弹层=**只读草稿预览**(主题+正文预写) + 邮件/复制到聊天/加入待办 | 见 2.1:裸 mailto,无预览弹层 | 补(=2.1) | 同 2.1 |
| `深入问 Nexus`:把决策问题 URL 带进对话自动问 | `带进议事室`:把卡带进画布 | 平级 | 两种"接力到推理面"的手法,可并存 |

### 3.4 议事室 / Nexus(推理面)—— 并存

| 她有的(Nexus 对话框) | 我们现状(议事室画布) | 判定 | 理由 / 证据 |
|---|---|---|---|
| 消息流(用户气泡+Nexus 回复) + 输入框 + 发送 + 打字指示 + 建议问题 chip + 追问 chip + `?q=` 自动问 | 画布(平移/缩放/重置) + "thinking it through" 终端 + 建议卡;提问框常驻画布外;4 个开场建议 | **并存** | `[LIVE]` 双方实测。Danny 指定并存:保留画布,补对话流体验 |
| 回复=**推理链 4 步**(读取事实/交叉验证/匹配方法/生成动作,永远全展开) + 回答 prose + 富件(团队负载表 / 6 柱营收图) + 建议行动卡 | 回复=**实时流式思考终端**(read_case/recall/cite/draft_advice,全量原样) + 8 字段建议卡;结果落 `Avery的笔记` | 补:**简化输出(见 2.2)** + 富件(图表类) | `[LIVE]` 我们思考流很生;她推理链更"给人看"但假(1200ms 假延时、canned)。我们真(真 minimax、真引用 facts.md),但要给简化视图 |
| 建议问题是固定 4 条(keyword 匹配 canned trace) | 开场建议 4 条;真 `/advise` 真答 | 平级(我们真) | 她 `nexus/page.tsx:53-73` 假延时+canned;我们真后端 |

---

### 3.5 人员 People

| 她有的 | 我们现状 | 判定 | 理由 / 证据 |
|---|---|---|---|
| `/people` 网格:组别 chip 筛选 + 情绪筛选 + 添加成员 + 停用/启用;卡显负载%/情绪/健康度 | `你的团队` **按团队分组**(7 组,每组可折叠) + 人卡显 name/role/`Owns …`;点开 `DetailOverlay` | 平级(呈现不同) | `[LIVE]` 我们 7 组:别墅销售组5/渠道合作组3/客户运营组4/市场投放组5/策略分析组2/活动策划组1;卡如"吴泽宇 商务拓展 Owns 外部合作渠道拓展,竞品定价与客源情报收集" |
| 人卡弹层:**ScoreRing + 5 DimensionBar(带分)+ TrendChart(范围切换)** | 无分数(见 2.4) | **不做** | 🔴 见 2.4 |
| 人卡:优势/待提升/协作/`Avery 的判断`/已标记 gap/参与项目(跳转) | `DetailOverlay`(本次未逐字段实测,`[CODE]` 有) | 补(候选,非评分向):"负责什么/协作谁/参与项目"深度 | 可把她人卡里**非评分**的"参与项目、协作、判断"补进我们 DetailOverlay |
| 人卡底部:`安排 1:1`(book 视图,日历深链) / `发消息`(草稿视图) | 无 book;起草=mailto | 补:1:1 排期(=3.3 book) + 草稿框(=2.1) | `[CODE]` `person-modal.tsx:496-500,507-751` |

### 3.6 项目 Projects

| 她有的 | 我们现状 | 判定 | 理由 / 证据 |
|---|---|---|---|
| `/projects` 看板:**添加项目**(内联表单) + 卡(状态/进度条/里程碑点/偏差红条/负责人+团队头像) | `你的团队` 项目区:16 张卡,状态(on-track/at-risk/blocked)+负责人+截止;点开详情 | 平级(她带增改) | `[LIVE]` 我们卡如"年度别墅营收冲刺 at-risk 陈思雨 7月17日拆解会" |
| **项目详情弹层**:内联编辑(名/影响/状态/截止) + 里程碑 + 偏差告警 + **成员卡可展开(当前工作+历史时间线)** + `归档`/`恢复` + `问 Nexus` | 项目详情(本次未逐字段实测) | 补(候选):成员时间线、里程碑、归档 | `[CODE]` 她 `project-detail-modal.tsx:72-374`;我们详情较轻。哪些值得补由 Danny 定 |

### 3.7 现实差距 / 对不上(她:/gaps 我们:多看一眼)

| 她有的 | 我们现状 | 判定 | 理由 / 证据 |
|---|---|---|---|
| `/gaps`:每卡 `自报情况` vs `Avery 观察到的`(红底证据) + `解决`/`忽略`/`加入待办`(跳转) + 历史 | `多看一眼`:每卡 `文件里的说法` vs `实际信号` + 负责人 + `厘清`/`先放一放`/`直接问本人↗`/`加入跟进` | 平级(我们更强) | `[LIVE]` 两边实测,高度同构;我们多"直接问本人→接力到议事室"。她"加入待办"只是跳页不真建条,我们"加入跟进"真写 flowStore |

### 3.8 待办 / 跟进(她:/checklist 我们:跟进)

| 她有的 | 我们现状 | 判定 | 理由 / 证据 |
|---|---|---|---|
| `/checklist`:当前/历史两视图,今天/本周/即将 分组,添加,勾完成/编辑/删除/恢复/清空历史;来源标(决策/nexus/gap) | `跟进`:进行中/已完成,今天/本周/稍后 分组,加到跟进,完成/内联编辑/删除/恢复;来源(晨间/议事室/快问/gap) | 平级 | `[LIVE]` 我们空态"把今早清单、议事室、快问里的事挪过来";`[CODE]` `flowStore`(localStorage 真持久)。基本对齐 |

### 3.9 方法库 / 操作手册(她:/playbooks 我们:操作手册)

| 她有的 | 我们现状 | 判定 | 理由 / 证据 |
|---|---|---|---|
| `/playbooks`:6 张方法论卡(图标/标题/说明/框架 chip),静态陈列;上手第 3 步选的方法驱动 | `操作手册`:**"即将推出"占位**("暂无操作手册"×3、"暂未接入") | 补(或明确定位为后置) | `[LIVE]` 我们是空占位屏。`[CODE]` 上手 step3 有 8 条方法目录选择机制,但 `PlaybooksScreen` 目前是 coming-soon。要么补内容,要么承认这屏后置 |

### 3.10 外壳 Shell(顶栏/搜索/通知/FAB/免责)

| 她有的 | 我们现状 | 判定 | 理由 / 证据 |
|---|---|---|---|
| 顶栏 7 路由 + 活跃 gap/待办 数字角标 | 顶栏 7 tab(无路由,Zustand `screen`);无角标 | 补(候选):tab 角标(待跟进/待看 计数) | `[CODE]` 她 `topbar.tsx:195-217` 角标是活数;我们无。角标能把"该看几条"显性化 |
| **全局搜索**(xl+):搜人/项目/页,下拉结果,回车跳转 | **无** | 补(候选) | `[CODE]` `topbar.tsx:220-254`。20 人 17 项目起就有检索需求 |
| **通知铃 + 下拉**(未读角标,全部已读,点跳转,逐条删) | `通知`(LiteBell,`[LIVE]` 显"7 条未读") | 平级(需对齐下拉细节) | 我们有铃+未读数;下拉内交互本次未逐条实测,`[CODE]` `notifyStore` 有 |
| **全局 FAB `问 Nexus`**(除 /nexus 外每屏常驻,展开内联输入→跳对话) | 无全局提问入口(提问在 你的团队/议事室 屏内) | 补(候选):全局快问 FAB | `[CODE]` `nexus-fab.tsx`。让"随处发起一问"更顺手 |
| 底部 5 条 AI 约束 chip(非最终依据/需人工复核/禁止单一人事处分/遵守数据权限/保留留痕) | 底部一句 disclaimer("只辅助不替你做决定…重大人事决策不能只拿它当依据") | 平级(措辞不同,精神一致) | `[LIVE]` 两边都有;她拆成 5 chip 更显性 |
| 单一浅色主题,无换肤 | `paper`/`aurora` 双皮(视觉 token,DOM/交互一致) | 我们独有(见 §4) | `[CODE]` `skin.ts` |

---

## 4 · 我们独有 / 她没有(别在对齐里弄丢)

| 屏/能力 | 说明 | 证据 |
|---|---|---|
| `未来方向`(产品宣言屏) | 讲清"demo 背后是什么产品"+ 预览未来(私密文件空间/skills/后台跑/红线闸)+ 复述红线 | `[LIVE]` |
| `Avery 的笔记`(观察实录) | 每次议事室真提问→Avery 用自己的话记一条观察,按天分组、可展开、回链到原问题,持久累积 | `[LIVE]` 实测:刚才那问的完整分析(带 facts.md 引用)已落笔记 |
| 议事室**画布**(平移/缩放/重置) | 空间化推理面,vs 她的线性对话框 | `[LIVE]` (并存,见 2.3) |
| **真后端 + 真数据**(非 mock) | 真上传→真 minimax 抽取→真引用 `facts.md:NNN`→租户隔离 owner_token | `[PAYLOAD]` `[LIVE]` |
| `paper`/`aurora` 双皮 | 一份 v02 两张皮(视觉 token 表切换,DOM/组件/交互一致) | `[CODE]` `skin.ts` |
| **红线是确定性闸**(非 prompt 承诺) | 不打分是结构性的(payload 无 score 字段 + 送答前确定性检查) | `[PAYLOAD]` `[LIVE]` |
| `直接问本人 ↗` / `带进议事室 ↗` | 从差距卡/closer-look 一键接力到推理面 | `[LIVE]` |

> 她相较我们独有的,主要是:全局搜索、全局 FAB、人卡分数/趋势(不搬)、项目增改归档+成员时间线、连接工具步骤、日历深链(book)。已在 §3 逐条列。

---

## 5 · 给 Danny 的建议第二波 + 开放问题

**建议优先补(强证据、合方向)**
1. **应用内草稿框(2.1)** —— 起草消息从裸 mailto 升级为"可预览/可复制到聊天/可加跟进"的弹层。Danny 点名。
2. **议事室简化输出(2.2)** —— 思考流给"简化/折叠/隐藏原始 tool 转储"视图,原始流可展开。Danny 点名;两边都缺,我们更该有。
3. **对话流并存(2.3/3.4)** —— 画布保留,补对话框式体验(或议事室内切换对话视图)。Danny 点名。
4. **行动集补齐(3.3)** —— book(约 1:1/会议深链)、委派型、确认型行动;人卡底部"安排 1:1"。

**候选(Danny 定要不要、排序)**
- 顶栏全局搜索(3.10)、全局问 FAB(3.10)、tab 角标计数(3.10)。
- 上手"连接工作工具"步骤(3.1)。
- 项目详情深度:里程碑/成员时间线/归档(3.6);人卡非评分深度:参与项目/协作/判断(3.5)。
- 首屏是否要一个"晨间聚合"(gap + 需关注人 + 决策)像她的指挥室(3.2)。
- `操作手册` 是补内容还是明确后置(3.9)。

**明确不做**
- 人卡 ScoreRing / DimensionBar / 业绩趋势打分(2.4/3.5)——🔴 硬约束 + 红线,payload 无分数字段。
- KPI 业务指标条(3.2)——无数据源;若未来有再说。

**开放问题(需 Danny 拍)**
- Q1 上手:整页向导(她) vs 浮层向导(我们)——保留哪种形态?
- Q2 "指挥室聚合首屏" 我们要不要?还是维持"你的团队 / 议事室"分屏?
- Q3 决策/差距/跟进 三者我们已分到 `今天值得你留意` / `多看一眼` / `跟进` 三处,要不要像她那样在首屏聚合一个"今天要决策的"?
- Q4 `操作手册` 的定位:近期补 or 归到 `未来方向` 的后置预览?

**本波边界(诚实标注)**
- 逐字段实测集中在:上手、你的团队(真数据)、议事室(真问答全流程)、跟进、多看一眼、操作手册、未来方向、笔记 —— 7 屏全走过 `[LIVE]`。
- `DetailOverlay`(人卡详情)、项目详情、通知下拉内部、AskCard 细节 本次未逐字段实测,取自 `[CODE]` 清单;需要的话第二波补现场。
- 合伙人版逐屏取自 `[CODE]` 全量清单 + `/`、决策队列、`/nexus` 现场;她的人卡/项目详情弹层内部取自代码。
- 遗留:只读工作树 `v02-baseline-ro` 里 `public/seed/` 放了 4 份 seed 供前端真上传用(该工作树是临时的,可随工作树一起清)。

*—— 第一波完，待 Danny 审。*

