# 验收手册 · 批次② （棒D–F：三件真部件）

> **状态：验收就绪。** 改动全在本地 commit（未 push）。人测点头后我才推。
> **验收 URL（必须带参）**：`http://localhost:5173/?v=2&mode=live&lang=zh`
> 批次① 手册见 `acceptance-1.md`（主页骨架）；本批在其上叠三件真部件。

## 机器判据（本批次收官 · 三处修复后重跑）

- `SPEC_STICK=5` cr 对齐规格：硬断言 **35/35 绿**（含批次① 的 13 行布局断言 + 三结构闸）
- A 区电池：**19/19 绿 exit 0**
- 全矩阵扫雷（9 屏 × 2 皮 × 4 世界）：**0 件 / 0 指纹 · REGRESSION 0**
- 四视角对抗审查逮到三处、已全部修掉并复验（见 §4）
- 对照板（本批已重拍）：`eval-harness/reports/align-board/2026-07-22/index.html`（desktop + mobile）

**dev server 三口挂着**：我方 `http://localhost:5173/?v=2&mode=live&lang=zh` · 她方 `http://localhost:3100/` · mock 后端 8137。

---

## 0. 本批做了什么（三件真部件 + 一个 bug 修复）

全部**零 transport 改动**、零后端改动——数据与链路都是现成的，本批是把它们做成她的形态。

| 件 | 是什么 | 数据来源 |
|---|---|---|
| ① 差距面板 | 主页右轨「文件自己对不上的地方」，配她 `/gaps` 的「自报 vs 观察」对照语法 + 三态筛选 chip | `deriveGaps(team)`（纯前端） |
| ② 关注成员面板 | 主页右轨「文件里反复提到的人」，首字母头像 + 姓名 + 角色 + 口径句 + ≤2 条原文 | `deriveAttentionPeople`（纯前端） |
| ③ 顶栏搜索 | ≥1366px 出现，检索成员/项目，Enter 跳第一条，无结果诚实，无材料时不出 | `searchTeam()` 纯客户端内存检索 |
| ④ 悬浮「问 Avery」 | 底部居中胶囊，输入 → 预填议事室（不自动发），无材料/有常驻输入框的屏收起 | `goScreen('room',{q})` 中继 |
| 🐛 B-2 修复 | 差距卡「自报」列不再拿标题行冒充自述 | `gapDerive` 加结构行闸 |

### 🐛 B-2 修复实证（你批次① 手册里看到的那个 bug）

差距卡「自报」列以前显示抽取层塞的标题行「项目：销售 FAQ 梳理」。现在加了闸：summary 若是
`项目：/负责人：/标题：` 这类**元信息行**就退回机械读出项目状态。实测（验收语料两张卡）：

| 差距卡 | 修复前「自报」 | 修复后「自报」 |
|---|---|---|
| 销售 FAQ 梳理 | 项目：销售 FAQ 梳理 | **按计划推进** |
| 客房夜床服务复核 | 项目：客房夜床服务复核 | **按计划推进** |

（上限修法——抽取层让 summary 优先取自述句——波及面大于本战役，已记债，本批只做下限。）

---

## 1. 端到端人测流程

起环境同 `acceptance-1.md` §1（mock 后端 8137 + preview 5173 + 她方 3100）。

### 第一段 · 空态（走 demo 门）
`http://localhost:5173/?v=2&mode=live&lang=zh` → 「用一份示例团队先看看」→ 指挥室。
**看点**：右轨两面板显示诚实空态；**顶栏搜索框**（≥1366px 宽窗口才出现，在铃铛左边）；
**底部居中「问 Avery」胶囊**。

### 第二段 · 满态（上传验收语料）
同一会话上传：
1. `eval-harness/tests/fixtures/cjk/Sanya_Team_Roster_ZH.md`
2. `.issues/layout-real-0722/acceptance-corpus/本周项目周报.md`

**逐件预期（实测值）**：

| 部件 | 预期 |
|---|---|
| 差距面板 | 2 张卡，「自报 vs 观察」对照双列；顶部三态 chip「活跃 2 / 已厘清 0 / 已搁置 0」，点 chip 切筛选 |
| 关注成员面板 | 3 行（孙浩 / 赵倩 / 陈思雨），**无百分比无血条**，每行带口径句 + 原文 |
| 顶栏搜索 | 输入「营销」或「陈」→ 分「人员 / 项目」两组结果，行内**零数字**；Enter 跳第一条详情；输入乱码 → 「没有匹配的成员或项目。」 |
| 问 Avery | 在指挥室输入一句 → 切到议事室、问题**已预填但没自动发**（`run.status` 仍 idle） |

### 逐屏 gate 行为（重要，别当 bug）

| 屏 | 顶栏搜索 | 悬浮「问 Avery」 |
|---|---|---|
| 指挥室 / 项目 / 待办 / … | ≥1366px 显示 | **显示** |
| 你的团队 | ≥1366px 显示 | **收起**（该屏底部本有「就你的团队提问」输入框，两个会撞位） |
| 议事室 | ≥1366px 显示 | **收起**（议事室有常驻 composer） |
| 任意屏 · 未上传材料 | **不渲染**（没材料没得搜） | **不渲染**（0721 事故：零数据提问会烧钱/断流被当故障） |

### 空/错态触发
- 搜索无结果：输入不存在的词 → 「没有匹配的成员或项目。」（≠ 无材料时的「先上传文件」）
- 无材料：清 `lite2:*` localStorage 刷新 → 搜索框和悬浮钮都整块不出
- 问 Avery 预填后不发：预填进议事室后**需要你手动点发送**才会真问 Avery（这是设计，不是没接线）

---

## 2. 与她的对照（刻意不同处，红线）

| 她有 | 我方 | 为什么 |
|---|---|---|
| 关注成员每行 91%/88%/… | **无百分比** | D14 人面零数字零血条；类型层就没数字槽位 |
| 搜索结果行带 `状态·进度%` | **零数字** | 我方 LiteProject 连 progress 都没有；人面更禁 |
| 悬浮球叫「问 Nexus」 | **「问 Avery」** | Nexus 是锁词；「快问」已被 feat-034 占用（你 07-22 拍板用「问 Avery」） |

---

## 3. ⚠️ 需要你过目 / 审字

### 🈶 中文文案未走 M3，是手写的——待你审字

本批 8 个新 i18n 键的中文是**手写**的，没走 `i18n-zh-delta.mjs`。
原因：那个脚本的 `JSON.stringify` 写法会连 `zh.ts` 里 8 行 feat-057 红线注释一起抹掉
（除非加 `--allow-comment-loss` 再手工贴回）——为几个极短标签冒抹掉红线注释的险不划算。
已在 `zh.ts` 头部加沿革注明。**请你审这几个词**（都短、无歧义，但按规矩中文该你过目）：

| key | en | zh（手写） |
|---|---|---|
| `searchPlaceholder` | Search people, projects… | 搜索成员、项目… |
| `searchAria` | Search people and projects | 搜索成员与项目 |
| `searchEmpty` | No matching people or projects. | 没有匹配的成员或项目。 |
| `askAveryLabel` | Ask Avery | 问 Avery |
| `askAveryAria` | Ask Avery about your team | 向 Avery 提问 |
| `askAveryPlaceholder` | Ask Avery about your team… | 向 Avery 提问… |
| `homeGapFilterActive` | Active | 活跃 |
| `homeGapsFilterEmpty` | Nothing in this state right now. | 这个状态下暂时没有。 |

（若你要走 M3 重译，说一声，我把 delta 脚本的注释保护补上再跑。）

---

## 4. 对抗审查逮到的三处（都已修 + 复验，你不用重复验）

四视角审查在三棒"全绿"之下逮到三个真问题，已全部修掉：

1. **button-family 门假绿**：差距三态 chip 是裸 button，门只绿在"没有差距卡"的种子世界
   （chip 没渲染就没被审计）。加载真语料后门会红。
   → 修：chip 进白名单。复验：真语料下 3 chip 渲染 + 0 裸按钮。
2. **英文顶栏 tab 被裁**：预算只算了中文 9 tab(770px)，英文是 978px，1280 EN 下搜索框
   把末 tab 挤进横向滚动（1366 笔记本 + EN 默认语种）。
   → 修：搜索 220→200px、断点 1280→1366。复验：EN 1280–1600 全程 tabs 978/978 不裁。
3. **悬浮钮撞团队屏输入框**：872 宽屏胶囊盖住「就你的团队提问」输入框正中。
   → 修：team 屏也收起悬浮钮。复验：home/projects 显示、team/room 收起。

---

## 5. 已知留后项

- **像素基线**：本批新增的满态部件（面板/搜索/悬浮）像素基线**零覆盖**（基线世界是 stub 空态，
  这些部件在空态不渲染）。且 `home-mobile` 基线是**批次① 之前的旧图**先天漂移（与本批无关）。
  → 基线补 seeded 世界 + 重冻是**独立一棒**，按纪律等你对照板点头后才 `--update-snapshots`。
- **switchers 门 flaky**：冷跑电池（dist 刚重建）偶发一次超时红，单跑/重跑即绿。非本批代码问题。
- **`steady` 状态前向提醒**：`gapDerive` 认 `on-track|steady` 为稳态，但 `projectStatusText`
  没有 `steady` 分支——当前**不可达**（后端从不吐 steady）。若日后扩抽取器产出 steady，需补键。
- **i18n 手写待审字**（见 §3）。
- push 待你人测点头（对外动作）。
