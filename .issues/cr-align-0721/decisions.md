# cr-align-0721 · 合伙人反馈 + Command Room 对齐 —— 拍板记录

日期：2026-07-21。输入：`C:\Users\86139\Documents\对话0721.txt`（合伙人用 codex 对
averylite.dannyqian.com 的零数据只读实测 + 改版建议 + Danny 回复）；对齐基准：
`D:\avery-command-room (2)\avery-command-room`（Next.js 版管理指挥室，**以 run dev 真机为准**；
本机无空格别名 junction：`D:\cr-live`，dev 端口 3100）。
三人探索队报告与 grill 过程见会话记录；本文件只记**拍板结果**——下一棒照此执行，不再重问。

## 探索队的总判读（背景，不是拍板）

合伙人是在**零数据**状态下测的（Chrome 扩展权限挡住了文件上传），她看到的「文件上传体验」
是 `!team` 空态分支——v02 有数据时的首页（feat-057）本来就是聚合指挥室。所以反馈大半是
**冷启动第一眼**问题，优先级 = 空态/示例团队 > 新机制。aurora 皮在 feat-046 已按 command-room
的 globals.css 逐值对齐过（同血缘），视觉对齐是把 aurora 做浓，不是推翻双皮。

## 九项拍板（Danny 2026-07-21，逐字依据他的回复「1A 2C 3A 4A 5B 6A 7B 8A 9A可能两棒都不够」）

| # | 决策 | 拍板 | 本棒落地 |
|---|------|------|----------|
| 1 | 「不打分」承诺 vs 已开评分开关 | **A · 改文案**：说明「评分系统可以打开/关闭」 | ✅ 全部 ≥6 处承诺点改为开关条件表述（en+zh，机制描述贴 scoring_policy.py 真实现） |
| 2 | 模块改名 | **C · 主+副小字折中**，具体方案授权我定 | ✅ home 主「指挥室/Command room」副「今天/Today」；followups 主「待办清单/To-do list」副「跟进/Follow-ups」；其余 7 tab 不动；「实时分析」不做 tab 名，做多看一眼页内预告 |
| 3 | 一键示例团队 | **A · 后端预铸共享 demo context**；除固定团队/项目/公司信息外，「实时数据缺位」也要在示例里呈现 | ⏭ 下棒（输入侧棒）；seed 原料已到位（见下） |
| 4 | 无数据首屏 | **A · 指挥室骨架** | ✅ 四块诚实骨架 + 上传降为右侧入口卡 + 登录提示前置；示例团队按钮插槽预留（不出假按钮） |
| 5 | 材料体检卡 | **B · 直接做后端真实版**（不做轻量过渡版） | ⏭ 下棒（棒级，动抽取层元数据） |
| 6 | 输出标签 | **A · 文案层统一**（事实/推断/建议/置信度/由你拍板） | ✅ 8 字段卡三 zone 标签显式化（两壳同值键） |
| 7 | 视觉对齐深度 | **B · 结构两皮共享、视觉只动 aurora**；追加：**aurora 转默认**、皮肤+语言切换器收进次级设置菜单 | ✅ 默认 look=aurora；⚙ 设置菜单（.lite-settings）；paper 数值零动 |
| 8 | onboarding 采集公司状况+项目详情 | **A · 送后端 + 承诺文案同步改** | ⏭ 下棒（company_notes 表已有；「不会发到任何地方」文案届时必须同步改——DoD 显式项） |
| 9 | 分棒 | **A+ · 两棒起步，可能更多** | 本棒=快改层+首屏+命名+视觉；下棒=输入侧三件套（示例团队+体检卡+onboarding） |

## 词表变更（对 0718 锁定词表 + ADR-0015 决策 4 的部分推翻——ADR-0025 记档）

- **解锁**：「指挥室」（home tab 主名即它）。verify-p0 锁定词断言已同步剔除。
- **维持锁定**：`Nexus`、「现实差距」——继续永不上屏。
- **保护性命名保留**：「多看一眼」不改名（被点名下属看到不刺痛的理由仍然成立）；
  Danny 的「实时分析」意图由页内预告承载（连接公司数据后变实时——条件时态，不许诺日期）。

## 下棒（输入侧棒）的既定输入

- **示例团队 seed（真实脱敏）**：`D:\Boyle\research\sanya-lushan-yiju-hotel\0721-脱敏seed\`
  ——1 份营销管理工作汇报.docx + 5 份虚构人员匿名简历.pdf。全中文文件名/内容。
  ⚠ 这套材料恰是「最小可运行档」（多简历+单周报），正好演示低置信度档位——与拍板 5B 的
  体检卡是同一个故事。⚠⚠ 会踩后端 issue #10（跨文档去重失效 + `_slug` 中文名压缩 u_x），
  **做 demo context 前必须先修 #10**，否则 5 个人名会塌成同一张卡。
- command-room 的酒店样本（`D:\cr-live\src\lib\data.ts`，滨海酒店 20 人中文故事线）可作
  体检卡「推荐档」的对照数据源；抄数据不抄代码。
- 反馈中的三档输入标准 + 六类误差识别设计质量很高（合伙人原文在对话0721.txt 后半），
  体检卡按它做真——后端抽取层输出覆盖度元数据，前端渲染，不做前端启发式冒充。

## 本棒顺手修掉的存量债（非拍板项，进 feat-081 证据）

- OnboardWizard ACCEPT 仍收 .doc/.xls 且漏 .tsv（fixB1 只修了 UploadPanel）——已同步。
- 议事室无材料提问烧默认 demo 语料 LLM / 被呈现成「中途断了」——UI 层 gate（store 缝不动）。
- 无材料空态 eyebrow 沿用「正在仔细梳理中」（此刻并没有在梳理）——换 tab 名。
