# kickoff-dev · v02 对齐波（partner-align）

规格见 `PRD.md`，拍板见 `decisions.md`。**子代理不得复议拍板**，有异议记 `progress.md` Notes。

## 架构拍板（不得复议）

1. **基线 = `main`**（当前 `d6c2791`）。main 已含全部 v02 工作（lite2 34 文件 + 中文修复五轮 + feat-042..049）。**不要从 feat/047 切**——那条已并入且已删。
2. **一条分支只在一个地方 checkout；main 永远待在 `D:/avery`。** 工作树用完即删（`git worktree remove` + `git worktree prune`），合并后立刻 `git branch -d`。上一波因为 main 被工作树占住，导致主检出永远上不了 main、基线判断全乱。
3. 前端 = `src/lite2/**`。**禁区**：`src/story/**`、`.issues/lite-v1-lean-real-0713/**`、`src/lite/**`（v01，不动）。
4. 后端 = `eval-harness/`。接线走 `VITE_AVERY_API_BASE`（生产 `https://avery.dannyqian.com`），`owner_token` 只走 header。
5. 锁定词表：`Nexus` / `现实差距` / `指挥室` 永不出现在用户面。搬她的交互可以，**搬她的词不行**。
6. 搬运参考库任何内容，**必须清掉写死的假数据**（假日期、假统计句、canned 延迟）。

## 并行策略（agent teams）

`feat-050 / 051 / 052 / 054 / 056 / 060 / 061` **彼此无依赖，可同时开**。
其余按下方依赖链排。**每条 feature 一个独立工作树 + 一条分支**，收工即删。

依赖链：
```
050 会话不丢 ─┐
051 路由化  ─┼→ 055 项目屏 ← 054 抽取粒度门
052 modal基座─┘         ↘
053 账号体系 ──────────→ 062 整页向导
056 决策定级 ──→ 057 聚合首屏
052 modal基座 ─→ 058 草稿框
```

---

# P0 —— 7/25 演示线

## feat-050 · 会话不丢（contextId 恢复）
`feat/050-session-restore` · 前端 · **最小最急，先开这条** · 无依赖

- **病因**：`src/lite2/store.ts` 的 `contextId` 初始化为 `null`、加载时不从 localStorage 恢复。后端早已持久化，`ownerToken` 也已存在 `lite2:ownerTokens:v1`，**只差 contextId**。
- **做**：存住 `contextId`；加载时按它走 `/team/{id}` 重新拉。handoff 原话「纯前端几行」。
- **验收**：上传 → 刷新 → 团队数据还在；关掉浏览器重开还在。
- 注：feat-053 落地后 contextId 来源改为服务端按账号返回，本条退化为兜底，不冲突。

## feat-051 · 路由化（router + 深链）
`feat/051-router` · 前端 · 地基，早开 · 无依赖

- **做**：引入 react-router，把 Zustand `screen` 变量换成真路由。
 7 屏：`/team` `/room` `/followups` `/notes` `/closer-look` `/playbooks` `/vision`
 深链：`/team/:personId`、`/projects/:projectId`
 接力：`/room?q=<问题>`（对应她的「深入问 Nexus」）
 软删除开关：`?showInactive=1`
- **必须不破坏**：现有 `?v=2&mode=live&skin=paper&lang=zh`。
- **别抄**：她的 `?highlight=id` 是死链（两个页面都不读该参数）。
- **验收**：深链复制到新标签页能直达；后退键在屏间正常回退、不掉出应用。

## feat-052 · 统一 modal 基座
`feat/052-modal-base` · 前端 · 地基（解锁 055/058）· 无依赖

- **为什么先做**：本波要加草稿框、项目详情、人卡详情多个弹层。**先统一基座再铺**，否则各写各的。
- **做**：单例 modal（背景点击关闭、Esc 关闭、滚动锁、动画）；把现有 `DetailOverlay` / `OnboardWizard` 收敛过来。
- **验收**：任意两个弹层行为一致（关闭方式、滚动、层级）。

## feat-053 · 账号体系（Supabase auth）
`feat/053-auth` · 前后端 · 无依赖（但 062 依赖它）

- **做**：Supabase auth（邮箱+密码）；注册/登录/登出/会话恢复。
- 🔴 **映射口径（别拆错）**：`Supabase user → 公司 context_id`。**`owner_token` 保留在下层不动**——服务端按 user 查 context 再取 token，前端不再手工持有。feat-038 租户隔离原样复用。
- **不做**：多角色权限、组织邀请、SSO/OAuth（Danny：不过度设计、不造轮子）。
- **要处理**：现有匿名 context 的归属——给"认领"路径或登录后新建，二选一。
- **验收**：两个账号各自登录只见自己公司的人/项目，换账号数据不串。

## feat-054 · 抽取粒度门（项目粒度收敛）
`feat/054-project-granularity` · 后端/抽取 · 无依赖（但 055 依赖它）

- **病**：handoff 挂着「项目粒度偏多（22–25 vs 应有 6），LLM 把里程碑当项目，无门」。实测三亚 seed 抽出 **17 个**，正是此病。
- **同源线索**：H4「中文项目轴幽灵」5 条 strict xfail —— **非 CJK bug，纯英文双项目文档也只抽出 1 个，是结构性问题**，一并查。
- **做**：给项目抽取加粒度门（里程碑 ≠ 项目），收敛到接近文档真实项目数。
- **验收**：三亚 seed 抽出的项目数落进合理区间；5 条 strict xfail 重新评估。
- ⚠️ **必须先于 feat-055**，否则项目屏一打开是一地碎片。

## feat-055 · 项目屏（整屏新建）
`feat/055-projects-screen` · 前端 · **依赖 051 + 052 + 054**

- **为什么**：差异清单最大结构缺口——**我们根本没有项目屏**，17 个真项目混在「你的团队」里当卡片。
- **做**：独立项目看板；卡片含 标题/状态/负责人/到期/阻塞；点开 `/projects/:id` 详情。
- 🔴 **必须设计「未知」态**：实测覆盖率 `title/summary` 17/17、`owner` 16/17、`status/blockers` 13/17、**`dueDate` 7/17、`progress` 6/17**。
 **绝不能把缺失渲染成 0% 或空白**——那等于把"文档没说"错报成"没有进展"。要显式显示「未知 / 文档未提及」。
- **不做**：里程碑、成员历史时间线（我们无此数据；她那份是 mock 造的）。
- **验收**：无 `progress` 的项目显示「未知」而非 0%。

## feat-056 · 决策定级（规则 + Avery 理由）
`feat/056-decision-grading` · 后端 · 无依赖（但 057 依赖它）

- **为什么需要**：她那三档标是**人手敲进假数据的**；我们的决策自动长出，必须有口径。
- **做（Danny 拍板 a+b）**：
 - 规则算等级：`高风险 / 需确认 / 可推进`（确定、可复现）
 - Avery 写那句人话理由（等级归规则、文字归它）
 - **允许 Avery 上调**（补规则盲区），必须写明为什么
 - 🔴 **禁止 Avery 下调** —— 漏报比误报贵，对方拿真文件来试
- **规则可用依据（实测覆盖率）**：`signals` 关键词 17/17 · `blockers` 13/17 · `status` 13/17 · `dueDate` 7/17 · `progress` 6/17。后两项覆盖只有 1/3 → **纯规则必有盲区，这正是留给上调的口子**。
- 🔴 **口径必须落成一份可读配置/文档，不许埋进 prompt**。三家公司一定会问"凭什么说这条高风险"，要能当场把表给他看。
- **验收**：同一份文件连跑两次等级完全一致；每条决策能展开看到命中了哪条规则。

## feat-057 · 聚合首屏 +「今天要决策的」
`feat/057-home-aggregate` · 前端 · **依赖 056**（排序要定级）

- **Danny 拍板**：聚合首屏和分屏**结合**，两个都极端 → 聚合做入口，**7 个分屏全部保留**为深入面。
- **做**：新增聚合入口屏，四块摘要，每块可点进对应分屏：
 ① **今天要决策的**（按 056 定级排序 + 待处理计数）← Danny 明确点名
 ② 差距摘要（→ 多看一眼） ③ 需关注的人（→ 你的团队） ④ 团队概览计数
- **不做**：她的 5 张 KPI 指标条（我们没那些量，见 feat-066）。
- 🔴 **尤其不要照抄她硬编码的问候统计句**（"扫描了 186 条信号""距国庆还有 83 天"全是写死的）。我们的 signals 是真的，要显示就真算。
- **「需关注的人」口径**：她用 `load>=90 或 sentiment=strained`，**我们两个字段都没有**。本波先用可得信号（关联 blockers、在 signals 中被提及次数），feat-066/067 落地后再换真口径。
- **验收**：聚合屏每个数字都能追到真数据来源，**没有一个是写死的**。

## feat-058 · 应用内草稿框
`feat/058-draft-composer` · 前端 · **依赖 052** · Danny 点名

- **现状**：「起草消息」是**裸 `mailto:` 链接**（`draftLinks.ts`），点了直接甩给系统邮件客户端，不弹任何应用内框。
- **她的**：只读预览弹层（收件人 chip + 主题 + 预写正文），出口 邮件/聊天应用(复制)/完成—加入待办。
- **做（对齐 + 三处超过她）**：
 1. 弹应用内草稿框：收件人 + 主题 + 正文
 2. 🟢 **正文可编辑**（她只读）——正文是 LLM 生成的，不可编辑等于不可用
 3. 🟢 **「复制到聊天应用」为主出口**（国内在微信/飞书，mailto 基本是死路），mailto 降为次出口
 4. 🟢 **「完成」真写进跟进队列**（她只弹 toast 不落库；我们有 `flowStore`）
- **不做**：Outlook / Google 日历深链（国内用不上）。
- **验收**：点「起草消息」弹框 → 能改正文 → 复制成功 → 完成后跟进队列真多一条且刷新仍在。

## feat-059 · 议事室简化输出
`feat/059-room-simplified` · 前端 · 无依赖 · Danny 点名 · ⚠️ **新造，不是照抄**

- **先纠正前提**：Danny 原以为"她有我们没有"。**核实：两边都没有**——她的推理链永远全展开，全库无折叠控件。这是**净新增**。
- **我们的问题**：`LiteTerminal` 把每条流事件逐行原样打（`read_case` / `recall query=…` / 大段 `facts.md` 转储 / `cite` / `draft_advice`），**英文脚手架 + 中文事实混排**，`streamSource.applyEvent` 完全不过滤 → 对经理是一堵开发视角的墙。
- 🟢 **设计结论**：**她那 4 步手写轨迹（读取事实/交叉验证/匹配方法/生成动作），正好是我们简化视图该收敛成的形状**。她的是假的（1200ms 假延迟 + canned），我们的是真的（真 minimax + 真引用 `facts.md:NNN`）——**把真流事件映射进那四相，等于把她的皮套在我们的实质上**。
- **做**：默认简化视图（四相叙事）+「展开原始流」。
- **注意别改错地方**：最终建议卡和「Avery 的笔记」**已经是干净中文**，脏的只有实时思考流。
- **验收**：默认视图无英文脚手架、无 `facts.md` 转储；展开后原始流一行不少。

## feat-060 · 红线补漏（name 字段 + `_NEG` 否定词）
`feat/060-redline-holes` · 后端/红线 · 无依赖

- 来自 handoff §仍开，**三家公司拿真文件来会当场撞上**：
 - 🔴 **`name` 字段未扫红线** —— 文件名兜底能把人命名成「绩效8分」
 - **`_NEG` 其他否定词同类洞** —— 当时只修了「别」，其余否定词同病
- **验收**：构造含评分词的文件名/人名，红线能拦；补齐否定词表并加回归测试。

---

# P1 —— 仍属本波，排在 7/25 之后

## feat-061 · 软删除（人停用 / 项目暂停）
`feat/061-soft-delete` · 前后端 · 无依赖

- **Danny 拍板**：可以删，但**软删除**——照她的样子：人可停用、项目可暂停，勾「显示已停用」仍可见。
- **做**：人 停用/启用、项目 暂停/恢复；显示开关走 `?showInactive=1`（配合 051）。
- 🔴 **必须真持久化落后端**——不能只 localStorage（人/项目状态要跨设备一致）。
- **验收**：停用后默认不显示、勾开可见；换设备/刷新一致。

## feat-062 · 整页向导
`feat/062-fullpage-onboarding` · 前端 · **依赖 053**（"创建账号"步要真账号）

- **Danny Q1 拍板：整页向导**。现状浮层 4 步 → 改独立整页（她自带 `layout.tsx` z-9999 盖住壳）。
- **步骤**：上传 → 连接工具 → 团队信息 → 选方法 → 创建账号。
- **「创建账号」步现在是真的了**（上一波因无账号体系明确不做，053 落地后成立）。
- ⚠️ **「连接工具」步有冲突**：上一波判定"假 OAuth 不诚实、不做"，Danny 本次"候选全要"。
 **折中：做，但必须带 `Coming` 标注，绝不做假的"连接成功"态**（诚实标注是仍生效的硬约束）。
- **保住我们已有、她没有的**：真 ingest（她上传是纯本地 mock，刷新即丢）、**断点续做**（×/Esc 记步；她刷新回第 0 步）、跳过=永不再来。
- **别抄的 bug**：她的 `canNext` 只做样式门控、**实际不拦点击** —— 我们要真拦。

## feat-063 · 议事室对话流（与画布并存）
`feat/063-room-chat-view` · 前端 · 依赖 059 较好（共用简化视图）

- **Danny 定调：两套并存，不二选一**。画布是我们独有的空间化推理面，保留为默认。
- **做**：议事室内增加「对话视图」切换。可借她的元素：消息流 + 用户气泡 + 输入条 + 建议问题 chip + 追问 chip + 打字指示。
- **不抄**：她的 1200ms 假延迟 + canned 回复——我们接真 `/advise` 流式。
- **验收**：同一问题在画布视图与对话视图都能问、结果一致。

## feat-064 · 操作手册内容化 + seed 入库
`feat/064-playbooks` · 前后端 · 无依赖

- **Danny Q4**：操作手册 = playbooks，近期补。已提供两份 mock（在 `e2e-seeds/`）：
 `hr_tools_database.json`（6 模块/10 工具，Danny 从 `D:\teammaster-master\` 给）、`酒店管理playbooks.json`（5 模块/10 工具，新建，选题对齐三亚决策面）。
- 🟢 **超过她的机会**：她的 playbook 是**死卡片**（纯展示不可点）。而 `hr_tools_database.json` 自带 `analysis_steps[]` + `ai_prompt_template` → 让 playbook **可执行**：选中方法 → Avery 真按该框架步骤跑一遍。从"方法库展示"升级成"方法库驱动分析"。
- **另**：建议卡应能点回方法库对应条目。
- ⚠️ **前置**：`e2e-seeds/` 被 `.gitignore` 排除，这两份 mock **只存本机、无任何备份**。先决定：调 ignore 规则入库，还是另找位置。

## feat-065 · 人卡与项目详情深度（非评分向）
`feat/065-detail-depth` · 前端 · 依赖 052

- 🟢 **关键**：这些**不含分数**，与 feat-067 **正交** —— 即使 067 延后，本条也能独立做，而且这才是人卡最有价值的一半。
- 人卡：优势 / 待提升、协作说明、「Avery 的判断」洞察卡、已标记 gaps、参与项目（可跳转）
- 项目详情：内联编辑、归档
- **别抄**：她的 `?highlight=id` 死链。

## feat-066 · KPI 推导引擎 + seed 补充
`feat/066-kpi-derive` · 后端 · 无依赖 · **Danny：算本波**

- 🟢 **不受人卡红线闸限制** —— 那套闸只拦"人身上的打分键"，业务 KPI 不沾，可独立开发。
- **两头都缺，都要补**：
 1. **缺工具**：`avery/tools.py` 全部工具只有 `read_case / recall / cite / draft_advice`，**无任何聚合/计算工具**。→ 新增 metrics/derive 工具：读原始条目 → 聚合 → **带出处**给出指标。
 2. **缺原料**：单次 ingest 只有一个时间点，算不出环比/趋势。→ 每次 ingest 存快照，建时间序列。
 3. **缺测试素材**：见下。
- **seed 补充（Danny "不够就加"）**：
 - **多期周报**（W28/W29/W30）—— 单期算不出趋势，也验证不了快照存储
 - **原始台账**（预订流水/投诉记录/营收明细，**只有明细、没有结论**）—— 现有 seed 全是结论式文档（"平均确认超 6 小时"）。不给纯明细，**永远分不清 Avery 是算出来的还是抄来的**
- **现实基线**：她 5 张 KPI 卡，我们目前只有「未解决差距」能真算。
- 🔴 **硬要求**：每个 KPI 必须能点开看"这个数是从哪几条算出来的"。做不到出处，就和她的硬编码假数据没区别。
- **验收**：喂一份只有明细没有结论的台账，Avery 能算出正确率值并给出算式。

## feat-067 · 人卡分数全链
`feat/067-person-scoring` · 全栈 · **本波最贵** · Danny 拍板本波做

- 政策已解禁（`AVERY_ALLOW_PERSON_SCORING`，生产 `=1`）。**但当初三层刻意加固，四处都要动**：

| # | 位置 | 要做的事 |
|---|---|---|
| 1 | `db/migrations/0001_avery_persistence.sql:67` `entities_person_no_scoring_keys`（被 `0002_person_keys_allowlist.sql` 升级为**白名单**） | **新写一条迁移**改掉白名单 |
| 2 | Python `put()` 值校验（"即使伪造合法键也没法把打分值偷渡过 put()"） | 放开对应校验 |
| 3 | 抽取层（实测人对象无 `score`，出现 0 次） | 新增分数字段产出 |
| 4 | `src/shared/i18n/zh.ts:103` `visionProofRedline` | 改文案（Danny：属 V0.1，一行字） |

- **UI**：ScoreRing（总分环）+ 5 条 DimensionBar（业绩产出/协作配合/主动性/可靠性/状态健康）+ 悬停证据 tooltip。
- **不做**：52 周 TrendChart（需时间序列；她那份是 `buildTrend` **合成假数据**）。
- 🔴 **硬要求**：每个分数**能追到证据**（点开看凭哪几条给的分）。凭空生成的分数在三家公司面前是负资产。
- **顺带**：H6「定性评分词过红线」2 条 strict xfail 要重新评估/转正（评分已可接受）。
- ⚠️ **知情提示**：本波唯一动数据库迁移的项；一旦上线，三家公司看到的是"Avery 在给我的人打分"——**这是产品定位的实质变化，不只是加个 UI**。
