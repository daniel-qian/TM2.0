# fixC · 「无法完成」被读成已完成

分支 `fix/zh-done-negation` · 工作树 `D:\avery-wt\fixC` · 从 `40ce59c` 切

**状态：done。** 一条 blocker + 顺带查出并一并修掉的同型问题三处，全部带回归门，
门在旧代码上实测 FAIL、修复后 PASS，两次输出都在下面。

---

## 1. 病：单字否定前瞻挡不住否定短语

`_norm_status` 的四条中文梯子各自带一个**单字**否定前瞻：

```python
_ZH_AT_RISK  = r"(?<![无没])风险|延期|..."
_ZH_DONE     = r"(?<![未没待])完成|已交付|..."
_ZH_ON_TRACK = r"进行中|推进中|(?<![不异])正常|按计划|如期|顺利|在轨"
_ZH_BLOCKED  = r"受阻|已阻断|阻塞|卡住|..."      # 一个前瞻都没有
```

单字前瞻只能往回看**一个**字，而中文用**短语**否定：「无法」的「法」、「未能」的「能」、
「没有」的「有」、「未发现」的「现」都不在那几个字表里，于是整句穿过前瞻直接命中关键词。

### 实测（跑出来的，不是读正则推的）

| 文档原话 | 修复前读作 | 性质 |
|---|---|---|
| 无法完成 / 未能完成 / 不能完成 / 没能完成 / 难以完成 / 无从完成 | `done` | 🔴 **反向** |
| 无法按时完成 / 未能如期完成 / 无法如期完成 / 本季度难以完成收尾 | `done` | 🔴 反向 |
| 未能如期交付 | `on-track` | 🔴 反向（命中「如期」）|
| 没有风险 / 未发现风险 / 无明显风险 / 无重大风险 / 不存在风险 / 未出现风险 | `at-risk` | 假警报 |
| 没有延期 / 不会延期 / 未见滞后 / 不存在逾期 / 没有落后 | `at-risk` | 假警报 |
| 未按计划推进 / 没有按计划推进 / 不按计划 / 未能如期 / 无法如期 | `on-track` | 🔴 反向 |
| 进展不顺利 / 不太顺利 / 不够顺利 / 不是正常 / 不太正常 / 不在轨 | `on-track` | 🔴 反向 |
| 未受阻 / 没有受阻 / 不存在阻塞 / 没有卡住 / 不会停工 / 未搁置 / 不再搁置 / 没有停滞 | `blocked` | 假警报 |

共 **48 条**。任务书点名要查的三条（「并非没有风险」「不是正常」、`_ZH_BLOCKED` 有没有同型不足）
全部查了，结论在 §4。

### 为什么是 blocker：下游把它反向放大

`status="done"` 正是 `decision_grading._m_done` 读的字段。整条链在**旧代码上**跑出来的原话：

```
文档：状态：本月无法完成
卡片：grade  = can_proceed（可推进）
      reason = 按规则判为可推进：项目自报已完成，且无风险信号。（未读到：进度、到期日——未知不等于没风险。）
```

一个自述「本月无法完成」的项目，Avery 告诉经理「已完成、可推进、无风险」。
不是漏报，是把客户自己写的话读成了**反面**。

---

## 2. 修：一个共用的、边界精确的否定扫描

四条梯子不再各自带前瞻，改共用 `_zh_negation_depth` / `_zh_states`
（`eval-harness/avery/ingest/extract.py`）。

**刻意不是滑动窗口。** `redline._negated` 用 32 字窗口，那正是 B3（「别墅」把红线整段关掉）的
成因：附近任何地方出现一个否定字，后面全部失效。这里否定必须**正好结束在关键词开始的位置**
才算数，所以下面这些**按构造**通过，不需要白名单：

| 词 | 读作 | 为什么不被误伤 |
|---|---|---|
| 未来风险较大 | `at-risk` | 「未」和「风险」中间隔着「来」，「来」不是连接字 |
| 非常正常 | `on-track` | 「非常」是 very |
| 不过完成了 | `done` | 「不过」是 but |
| 无论如何都要如期交付 | `on-track` | 「无论」是 regardless |

（B3 的结论原话是「别墅白名单几乎什么都修不了」。这里不用白名单是同一条道理。）

### 两处刻意的不对称

**其一：`_ZH_CANNOT_DELIVER` → at-risk，不是留白。**
「未完成」是中性事实（还没做完 = 正在做），保持留白；「无法完成」是项目在告诉经理它会 miss。
把后者压成留白是诚实但装聋，而且那个留白到了 decision_grading 就变成「文档没陈述状态」——
对一份写得清清楚楚的文档说这句话，本身又是一次失实。所以「无法/未能/不能/没能/难以/无从」+
（按时/按期/如期/准时/及时）+（完成/交付/上线/结项/验收/完工/竣工/收尾/达成）→ at-risk。
这是**读出来的，不是编出来的**。

**其二：双重否定，正负两侧不同待遇。**
`_zh_negation_depth` 数层数而不是给 True/False，因为中文双重否定的两个方向意思相反：

- 「并非没有风险」= 确实有风险 → `at-risk`（depth 2 当 depth 0 用）
- 「不无风险」= 有风险 → `at-risk`（修复前读成留白，「无」把它挡掉了）
- 「并非无法完成」= 还有机会做完 **≠**「已完成」→ 留白（正面档 `positive_claim=True` 要求 depth 严格为 0）

正面档（done / on-track）要求 depth 严格为 0，风险档只要求偶数。理由和 `risk_only` 那段一样：
可推进必须建立在**明确的正面自报**上，一句反驳不是自报完成。

保留的两个前瞻 `(?<![待])完成` / `(?<![异])正常` **不是否定**（「待」是 pending，「异」是
abnormal），刻意留在各自模式里，没有混进共用机制。

---

## 3. 门：旧代码 FAIL / 修复后 PASS

新增 `eval-harness/tests/test_zh_status_negation.py`，**96 个用例，全部真汉字**
（上一波栽在「整套门语料是 ASCII 拼音伪装」——拼音语料根本测不到 CJK 无词边界，
而无词边界正是这个 bug 的全部成因）。

八张表：四张 BORN RED（四条梯子各一张）、`_STILL_READS` 控制组、`_LOOKALIKES` 误报侧、
`_DOUBLE_NEGATION`、`_ENGLISH`。

### 旧代码上（`git show HEAD:...extract.py` 覆盖回去实跑）

```
$ python -m pytest eval-harness/tests/test_zh_status_negation.py -q
54 failed, 42 passed in 1.48s
```

其中整条链那一条：

```
E  AssertionError: 一个自述「本月无法完成」的项目被定级 可推进：
   '按规则判为可推进：项目自报已完成，且无风险信号。（未读到：进度、到期日——未知不等于没风险。）'
E  assert 'can_proceed' != 'can_proceed'
```

### 修复后

```
$ python -m pytest eval-harness/tests/test_zh_status_negation.py -q
96 passed in 0.53s
```

### 全量后端

```
$ python -m pytest eval-harness/tests/ -q
3052 passed, 61 skipped, 4 xfailed in 22.08s
```

基线 2956 passed / 0 failed / 4 xfailed → 2956 + 96 = 3052，**0 failed，一条没跑红**。

### 门自己也要证明自己咬得动

- `test_every_born_red_case_flips_under_the_pre_fix_ladder` —— 把 extract **就地**退回修复前
  （常量换回原文 + `_zh_states` 换回裸 `re.search`），逐条断言它给出当年实测的那个**错**答案。
  语料表第三列就是这么来的。任何一条不再翻转 = 它已经不在测这个 bug，该删而不是留着看着像保护。
  抄的是 B3 那份文件立的规矩。
- `test_the_same_weekly_used_to_be_graded_can_proceed` —— 同样的突变，跑整条链，
  断言旧代码给出「可推进 / 自报已完成」那句原话。
- `test_the_negation_scan_is_boundary_exact_not_a_window` —— 直接对 `_zh_negation_depth` 提问，
  把「边界精确」这条**机制**钉住，而不是钉住它当前的几个后果（后果会随词表变，机制不会）。

### 误报侧代价：实测 0

`_STILL_READS`（30 条）+ `_LOOKALIKES`（6 条）+ `_ENGLISH`（8 条）在修复前后**读数完全不变**。
81 条语料的 before/after 全表都跑过，只有该翻的 50 条翻了。没有这张控制组，上面四张 BORN RED
毫无意义——把四条梯子整个删掉也能让它们全绿。

### 前端门（本轮没动任何前端文件，仍跑了）

```
$ npm run typecheck     tsc -b，0 错
$ npm run lint          ✖ 5 problems (0 errors, 5 warnings)   —— 5 条均为既有 warning，非本轮引入
$ npm run build         ✓ built in 3.32s
```

---

## 4. 顺带核查的结论（任务书点名）

| 查的 | 结论 |
|---|---|
| `_ZH_BLOCKED` 前瞻不足？ | **是，而且最严重——它一个前瞻都没有。** 未受阻/没有卡住/不会停工 全部读成 blocked。已修。方向偏安全侧不等于可以留着：对一份写着「未受阻」的文档说「已阻塞」，同样是产品在说客户文件里没有的话，只是这次说的是坏消息。 |
| `_ZH_AT_RISK`「并非没有风险」？ | 修复前**碰巧对**（`at-risk`）——「有」不在 `[无没]` 里，双重否定的第二层漏过去反而得到正确答案。修复后靠 depth 计数**有理由地**保持 `at-risk`，已钉死在门里。 |
| `_ZH_ON_TRACK`「不是正常」？ | **是，错的**（`on-track`）。同族一整批（不太正常/进展不顺利/不太顺利/不够顺利/不按计划/未能如期）全错。已修。 |
| 054×056 合并时收敛掉的东西有没有被加回来？ | 读了 `3ef4224` 的 merge 说明和 `extract.py` 近 15 条历史。当时删掉的是**第二层无前瞻的梯子**；本轮没有恢复任何第二层，改的是**唯一那条梯子的否定机制**，`_ZH_BLOCKED` 里 054 独有的 受阻/已阻断 原样保留。 |

---

## 5. Notes（发现了但**没动**——不在本线文件边界内）

1. **`_m_no_evidence` 的理由文本**（`avery/decision_grading.py`）。
   「未按计划推进」这类现在诚实地读成 `""`。若该项目其余字段也全空，就会落到 no-evidence 规则，
   理由说「status / blockers / progress / dueDate 全部缺失」——对一份**写了**「未按计划推进」的
   文档而言，这句话仍然不准确。属于同一个主题（"我没读到" vs "客户说没有"）的下一层，
   但文件不在 fixC 边界内。**未动。**

2. **「并非无法完成」→ `""`**。语义上更接近 on-track（"我们还能做完"），现在给留白。
   诚实但保守，构造极罕见，本轮不扩大射程。已在门里钉住当前行为，改起来是一行。

3. **`test_no_rule_text_in_any_prompt` 是条真门。** 本文件第一版在断言里写了规则号字面量，
   被它当场抓住（规则号只许住在 decision_rules.py / decision_grading.py /
   decision_grading_rules.md）。已改成断言**用户看得见的东西**（等级、理由、证据文本），
   比断言规则号更强。记在这里是因为下一个写决策相关测试的人会踩同一脚。

4. **`_ZH_NEG_LINK` 是可枚举的白名单，不是完备的中文否定文法。** 没覆盖的连接字会退回
   "不识别为否定"，即**回到修复前的行为**，不会产生新的错误方向。加词是安全操作，
   加完请在 `_LOOKALIKES` 里同步加对照。

---

## 6. 改动的文件

- `eval-harness/avery/ingest/extract.py` （+92 / −9）
- `eval-harness/tests/test_zh_status_negation.py` （新增，96 用例）

边界内，未越界。
