# v02 对齐波 · 拍板记录（2026-07-18）

子代理不得复议。有异议记 `progress.md` Notes，不要自行改判。

## 背景

第一波差异清单（`../lite-live-v02-0713/_diff-audit/first-wave-diff.md`）出完后，Danny 逐条审。
本文件是那一轮的全部结论。

## 一、对原差异清单 §5 的答复

| 原文提问 | Danny 拍板 |
|---|---|
| 建议优先补（4 条） | **全要** |
| 候选（5 条） | **全要** |
| Q1 上手：整页向导 vs 浮层向导 | **整页向导** |
| Q2 要不要"聚合首屏" | **两个都极端 → 结合**：聚合做入口，7 个分屏保留为深入面 |
| Q3 要不要在首屏聚合"今天要决策的" | **要** |
| Q4 操作手册定位 | **就是 playbooks，近期补**。提供 `hr_tools_database.json`，另做 `酒店管理playbooks.json` mock |

## 二、Danny 主动推翻的两条「明确不做」

| 原判 | 新拍板 |
|---|---|
| 人卡评分套件「不做（硬约束+红线）」 | 🔄 **本波做**（G10）。理由：打分门闸已确认软化；"已公开承诺"不成立——现在没有真实用户，`visionProofRedline` 只是 V0.1 文案，一行字。**一切以三家外部方的反馈为准。** |
| KPI 数字条「不做（无数据源）」 | 🔄 **算本波**（G11）。Danny 指出 KPI 不该依赖文件直给，应由 Avery 自己分析——核实属实：**agent 确实缺这个工具**。 |

## 三、本 session 新增的 6 问

| # | 问题 | 拍板 |
|---|---|---|
| 1 | 人卡分数本波做还是 7/25 后 | **本波做** |
| 2 | KPI 推导本波还是单开 feat | **算本波** |
| 3 | 上不上 router | **上** |
| 4 | 要不要真账号体系 | **要。Supabase auth，不过度设计、不造轮子。**每家公司自己的账号，之后可能发多家 |
| 5 | 人/项目能否手动增删 | **可以删，但软删除**。照她的样子：人可停用、项目可暂停，勾"显示已停用"仍可见 |
| 6 | 决策定级规则谁定 | **a+b**：规则定级 + Avery 写理由。**Avery 只许上调不许下调**，口径落成可读配置、不埋 prompt |

## 四、时间线与终点

- **2026-07-25 前**完成部署内测，之后把 Avery app 地址发给：三亚公司（鹿山雅居）、瑞典建筑公司、国内融资团队
- 三家会**拿自己的真文件**来试
- 前后端部署工作彼时并行推进中
- 本波边界已明确变广；PRD/ISSUES 开完后**用 agent teams 并行开发**

## 五、agent 提出但被否决/降级的顾虑（留痕）

- 「`visionProofRedline` 是已上线的公开承诺，不宜推翻」→ **Danny 否决**：那只是 V0.1 文案，且尚无真实用户。已采纳为"改一行字"。
- 「人卡分数最贵，建议放 7/25 之后」→ **Danny 否决**：本波做。（PRD 中仍标为 P1，即"本波内、7/25 演示之后"，如需提前请等量移出 P0 项。）

## 六、仍然有效的继承约束（不得放松）

- 锁定词表：`Nexus` / `现实差距` / `指挥室` **永不出现在用户面**
- 「问 AI」（议事室）与「快问」（向员工收自述）不得混用
- 未实现内容必须带 `Preview` / `Coming` / `Mock` 诚实标注
- 搬运参考库内容必须**清除写死的假活数据**
- `VITE_AVERY_API_BASE`（生产 `https://avery.dannyqian.com`）绝不写死；`owner_token` 只走 header，缺/错 → 404
- 禁区：`.issues/lite-v1-lean-real-0713/**`、`src/story/**`
